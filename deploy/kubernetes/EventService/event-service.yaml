apiVersion: v1
kind: Service
metadata:
  labels:
    app: eventservice
    env: testing
  name: eventservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: eventservice
  type: ClusterIP
