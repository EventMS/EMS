apiVersion: v1
kind: Service
metadata:
  labels:
    app: clubMemberservice
    env: testing
  name: clubMemberservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: clubMemberservice
  type: ClusterIP
