apiVersion: v1
kind: Service
metadata:
  labels:
    app: identityservice
    env: testing
  name: identityservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: identityservice
  type: ClusterIP
