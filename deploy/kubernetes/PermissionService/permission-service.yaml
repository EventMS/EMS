apiVersion: v1
kind: Service
metadata:
  labels:
    app: permissionservice
    env: testing
  name: permissionservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: permissionservice
  type: ClusterIP
