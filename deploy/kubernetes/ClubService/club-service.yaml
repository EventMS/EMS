apiVersion: v1
kind: Service
metadata:
  labels:
    app: clubservice
    env: testing
  name: clubservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: clubservice
  type: ClusterIP
