apiVersion: v1
kind: Service
metadata:
  labels:
    app: eventParticipantservice
    env: testing
  name: eventParticipantservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: eventParticipantservice
  type: ClusterIP
