apiVersion: v1
kind: Service
metadata:
  labels:
    app: eventverificationservice
    env: testing
  name: eventverificationservice
  namespace: eventms
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: eventverificationservice
  type: ClusterIP
