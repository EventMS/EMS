apiVersion: v1
kind: Service
metadata:
  name: {{ template "clubapi.fullname" . }}
  labels:
    app: {{ template "clubapi.name" . }}
    chart: {{ template "clubapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "clubapi.name" . }}
    release: {{ .Release.Name }}
