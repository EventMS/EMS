extend type Club {
  user: identity_ApplicationUser @delegate(schema: "identityapi", path: "user(id: $fields:adminId)")
  clubsubscription: [ClubSubscription] @delegate(schema: "subscriptionapi", path: "subscriptionsForClub(clubId: $fields:clubId)")
}
